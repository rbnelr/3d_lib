
// empty, since i put the whole engine codebase into headers to include, since i dislike having to seperate code into headers and sources
//  but i make it so that the dependencies of the engine (glfw is the only one that is not header only) get compiled into a .lib of my engine that can then be linked with the user code
